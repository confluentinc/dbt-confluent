Recover from a crash between `streaming_table`'s DDL and INSERT: a follow-up `dbt run` (no `--full-refresh`) now resubmits the missing INSERT instead of silently skipping the model. See #32 for the full lifecycle change.

Addressed the findings from the code quality review [#144]. Unit tests were added for `utils`, `impl`, and `column` to bring coverage from 73.7% to 82.3%, and the 80% floor now lives in `[tool.coverage.report]` and is enforced in CI by running `pytest --cov`.

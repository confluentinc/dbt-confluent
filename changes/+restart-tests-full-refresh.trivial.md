Fix the streaming_table restart tests (`TestCrashRecoveryRestart`, `TestDeadStatementRestart`) so they actually exercise the restart path. The shared test fixture sets `+full_refresh: True`, which makes `should_full_refresh()` true for every run and forces the create path — so the second run was silently full-refreshing instead of restarting (the assertions passed coincidentally). The classes now override `project_config_update` to drop `+full_refresh`, and assert on the restart-path log line via `run_dbt_and_capture` so a future regression fails loudly.

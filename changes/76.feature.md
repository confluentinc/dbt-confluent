Add a `statement_properties` config for `streaming_table` to set Flink SET-style statement properties (e.g. `sql.tables.scan.idle-timeout` to tune progressive idleness detection for a temporal join whose enrichment side rarely changes). Applies to the long-running INSERT statement only — never the separate DDL statement, which has no query to tune. See MATERIALIZATIONS.md.

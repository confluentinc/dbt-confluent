`tableflow.retention_ms`/`data_retention_ms`/`error_handling` now nest under a `tableflow.config` mapping, mirroring Tableflow's own `spec.config` shape verbatim instead of a flattened dbt-invented one — e.g. `tableflow={'config': {'retention_ms': ...}}` instead of `tableflow={'retention_ms': ...}`. See MATERIALIZATIONS.md#tableflow.

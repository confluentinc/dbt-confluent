Support dbt contracts (`config(contract={'enforced': true})`) for the `materialized_table` materialization, including model-level `primary_key` constraints, which render as `PRIMARY KEY (...) NOT ENFORCED` in the submitted `CREATE OR ALTER MATERIALIZED TABLE` DDL — enabling snapshot queries against the resulting table's key. Models without an enforced contract are unaffected. See MATERIALIZATIONS.md.

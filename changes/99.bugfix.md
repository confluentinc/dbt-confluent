A model that configures `tableflow` on a profile with only a Flink-region key now fails with a clear error naming `global_api_key`/`global_api_secret` as the field to add, instead of the raw driver error, which references a `connect()` parameter (`database_kafka_cluster_id`) this adapter doesn't expose through `profiles.yml`. Only this specific, recognized driver error is rewritten -- any other `ProgrammingError` still surfaces unchanged.

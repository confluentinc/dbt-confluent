A model that configures `tableflow` on a profile that can't reach Tableflow's control plane (a Flink-region key with no Global or `tableflow_api_key`/`tableflow_api_secret` pair) now fails with a clear error naming the profile fields to add, instead of the raw driver error, which references a `connect()` parameter (`database_kafka_cluster_id`) this adapter doesn't expose through `profiles.yml`.

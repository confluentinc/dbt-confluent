Sped up the `distributed_by` and schema-drift functional suites by collapsing redundant live `dbt run` cycles and dropping per-kind/per-materialization cases that duplicate the (pure-Python) unit coverage in `test_check_schema_drift.py` and `test_validate_distributed_by_config.py`. Functional drift tests now prove only the end-to-end wiring (one representative drift per code path), cutting expensive streaming_table setups roughly in half. No user-facing behavior change.

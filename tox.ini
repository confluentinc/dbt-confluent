[tox]
skipsdist = True
envlist = py38,py39,py310,py311

[testenv:{unit,py38,py39,py310,py311,py}]
description = unit testing
skip_install = true
passenv =
    DBT_*
    PYTEST_ADDOPTS
commands = {envpython} -m pytest {posargs} tests/unit
deps =
  -rdev-requirements.txt
  -e.

[testenv:{integration,py38,py39,py310,py311,py}-{ confluentcloud }]
description = adapter plugin integration testing
skip_install = true
passenv =
    DBT_*
    CONFLUENTCLOUD_TEST_*
    PYTEST_ADDOPTS
commands =
  confluentcloud: {envpython} -m pytest {posargs} -m profile_confluentcloud tests/integration
  confluentcloud: {envpython} -m pytest {posargs} tests/functional
deps =
  -rdev-requirements.txt
  -e.
